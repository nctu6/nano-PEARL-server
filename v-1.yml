services:
  v-1:
    image: vllm/vllm-openai:latest

    # GPU access
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

    network_mode: host
    shm_size: '2gb'

    volumes:
      - /home/ubuntu/models:/root/models

    environment:
      CUDA_VISIBLE_DEVICES: 0,1

    command: >
        /root/models/Qwen/Qwen3-32B --served-model-name=test --tensor-parallel-size=1 --host=0.0.0.0 --port=30000 --max-model-len=8192

